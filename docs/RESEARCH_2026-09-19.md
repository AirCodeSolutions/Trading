# Research log — 2026-09-19

## Objet

Premier passage du Regime-Conditional Opportunity Engine sur les historiques MT4 disponibles, avec contraintes de capital de référence de 200 EUR.

Aucun résultat de ce document n'autorise une exécution. Les trois mécanismes restent expérimentaux.

## Données

Historique :

- M5 et M15 fermés, issus en priorité de `mt4_research_bars_*` ;
- train avant 2026-07-01 ;
- validation du 2026-07-01 au 2026-09-01 ;
- holdout à partir du 2026-09-01.

Spécifications d'exécution :

- BTCUSD : snapshot broker courant au moment du test ;
- XAUUSD et EURUSD : derniers snapshots broker observés disponibles ;
- capital économique : 200 EUR ;
- slippage modèle : 0.25 spread ;
- spread historique tick-par-tick indisponible : le backtest applique le spread snapshot constant.

Cette dernière limite interdit d'interpréter les résultats comme une reproduction parfaite des coûts historiques.

## Mécanismes

1. `post_shock_continuation`
2. `break_retest_reaccel`
3. `failed_auction_reversal`

Le signal est formé à la clôture. L'entrée est toujours sur la bougie M5 suivante.

## Politique 1 % par trade

Aucun couple marché × mécanisme n'est ACTIVE.

### BTCUSD

- post-shock : 166 trades exécutés, holdout expectancy -0.339R ;
- break/retest : 49 trades, validation +0.116R mais holdout -1.000R sur seulement 2 trades ;
- failed-auction : 276 trades, validation -0.137R et holdout -0.115R, REJECTED.

Les rejets BTC sont principalement dus à un spread trop grand relativement au stop et, secondairement, au lot minimum.

### EURUSD

Les trois mécanismes restent négatifs. Le coût relatif du spread élimine également une grande part des setups avant exécution.

### XAUUSD

Le lot minimum 0.01 rend la majorité des stops incompatibles avec un budget de risque de 2 EUR :

- post-shock : 0 trade exécutable ;
- break/retest : 0 trade exécutable ;
- failed-auction : 4 trades seulement.

Le résultat positif de ces 4 trades n'est pas une preuve exploitable.

## Test de sensibilité au plafond 2 %

Le plafond absolu de 2 % a été testé uniquement pour mesurer l'effet de la granularité de lot.

Aucun couple ne devient ACTIVE.

Observations principales :

- XAU failed-auction : 221 trades exécutés, validation +0.028R mais holdout -0.112R sur 32 trades => REJECTED ;
- XAU break/retest : 23 trades, validation +0.167R, holdout -0.067R sur 3 trades => SHADOW insuffisant et non stable ;
- BTC post-shock : 260 trades, validation +0.132R mais holdout -0.397R => REJECTED ;
- BTC failed-auction : validation -0.185R => REJECTED ;
- BTC break/retest : validation +0.241R sur 16 trades et holdout +0.400R sur 6 trades, mais échantillon indépendant trop faible => SHADOW ;
- EURUSD reste négatif sur les trois mécanismes.

Conclusion : augmenter le risque ne transforme pas un mécanisme faible en mécanisme rentable. Le risque de base reste 1 %.

## Signal de recherche prioritaire

Le seul mécanisme qui mérite une investigation ciblée supplémentaire est :

`BTCUSD / break_retest_reaccel`

Raison :

- amélioration validation + holdout ;
- comportement récent différent du train ;
- échantillon encore trop petit pour conclure ;
- coûts et granularité restent des contraintes importantes.

La prochaine étape n'est pas d'augmenter le risque ni d'abaisser les garde-fous, mais d'expliquer causalement **dans quels sous-régimes** ce mécanisme récent fonctionne ou échoue.

## Règle de décision

Tant qu'aucun couple n'est `ACTIVE`, la matrice portefeuille renvoie explicitement :

`qualified_strategy_id = null`

Le moteur de recherche ne peut donc pas autoriser une exécution à lui seul.
